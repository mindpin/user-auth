.sign-in-form
  = simple_form_for User.new, :as => :user, :url => '/account/sign_in' do |f|
    = f.input :login, :placeholder => '用户名', :required => true, :as => :string
    = f.input :password, :required => true, :placeholder => '密码', :required => true
    = f.button :submit, '登录'
    = f.input :remember_me, :as => :boolean